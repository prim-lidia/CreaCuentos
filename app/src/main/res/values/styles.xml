<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryTopDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/primary_text</item>
        <item name="android:editTextStyle">@style/CustomEditText</item>

        <!-- specify shared element transitions -->
        <item name="android:windowSharedElementEnterTransition" tools:targetApi="lollipop">
            @transition/changebounds</item>
        <item name="android:windowSharedElementExitTransition" tools:targetApi="lollipop">
            @transition/changebounds</item>

    </style>
    <!-- Button styles -->
    <style name="Button.Rounded" parent="Base.Widget.AppCompat.Button">
        <item name="android:textAllCaps">false</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:minHeight">30dp</item>
        <item name="android:paddingTop">8dp</item>
        <item name="android:paddingBottom">8dp</item>
        <item name="android:paddingLeft">@dimen/button_padding</item>
        <item name="android:paddingRight">@dimen/button_padding</item>
    </style>

    <style name="Button.Rounded.Accent" parent="Button.Rounded">
        <item name="android:background">@drawable/button_rounded_accent</item>
    </style>
    <style name="Button.Rounded.Primary" parent="Button.Rounded">
        <item name="android:textColor">@color/icons</item>
        <item name="android:background">@drawable/button_rounded_primary</item>
    </style>
    <style name="Button.Rounded.White" parent="Button.Rounded">
        <item name="android:background">@drawable/button_rounded_white</item>
        <item name="android:textColor">@color/colorPrimaryDark</item>
    </style>

    <style name="EditText.Login" parent="Base.V7.Widget.AppCompat.EditText">
        <item name="android:textColor">@color/icons</item>
        <item name="android:textColorHint">@color/fiftyWhite</item>
        <item name="android:backgroundTint" tools:targetApi="lollipop">@color/icons</item>
    </style>

    <style name="CustomEditText" parent="Widget.AppCompat.EditText">
        <item name="android:layout_marginRight">-2dp</item>
        <item name="android:layout_marginLeft">-2dp</item>
    </style>
</resources>
